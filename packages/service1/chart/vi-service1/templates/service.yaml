apiVersion: v1
kind: Service
metadata:
  name: {{ include "vi-service1.fullname" . }}
  labels:
    {{- include "vi-service1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vi-service1.selectorLabels" . | nindent 4 }}
